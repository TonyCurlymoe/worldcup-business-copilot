# Data

This directory stores local datasets, embeddings, exports, and cached third-party payloads used by the copilot. The default application runs with demo data from `src/services/data.py` so it can start without paid API keys.
